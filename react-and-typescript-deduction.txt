question:
prop types are... deprecated approach?


Instruktor nie mówi „nie typuj propsów" — mówi „nie używaj PropTypes". To dwie różne rzeczy.

PropTypes to runtime'owa walidacja z czasów przed TS:

js
NameTag.propTypes = { name: PropTypes.string, isOnline: PropTypes.bool };
Sprawdzała typy dopiero w przeglądarce, w dev buildzie, przez console.error. Wyleciała z core React (osobna paczka prop-types), a w React 19 wsparcie usunięto całkowicie. TypeScript robi to samo lepiej i wcześniej — w edytorze. Więc tu ma rację: temat martwy.

Type vs Interface

z roku na rok stają się do siebie co raz bardziej podobne

największa różnica polega na extending an interface or a type

interface może rozszerzać nie tylko inny interaface ale również type

rozszerzanie interface'ow:
interface Employee {
  name: string;
  title: string;
}
interface NameTagProps extends Employee {
  level: number;
  isOnline: boolean;
}

ale jeśli to type ma rozszerzać type to wtedy składnia jest nieco inna:

type Employee = {
  name: string;
  title: string;
};
type NameTagProps = Employee & {
  level: number;
  isOnline: boolean;
};

Nazywa to się Intersection

Niuansem jest nadpisywanie rzeczy
bo w interface nie można nadpisać pola innym typem
a w intersection nie ma błedu kiedy się nadpisze

type Employee = {
  name: string;
  title: string;
  level: number;
};
type NameTagProps = Employee & {
  level: number;
  isOnline: boolean;
};

Another comparison: Interfaces can only describe object shapes and, if an interface is declared multiple times, then it merges with previous declarations.

skonczone na quiz types vs interfaces




Sposoby na otypowanie children jakie przyjmuje react component
children: React.ReactNode

ale łatwiejsze do zapamiętania jest 

{ children }: PropsWithChildren

dzięki tym dwóm rozwiązniom react component może przyjmować:
- zwykły string
- HTML który nie jest nested
- HTML element
- więcej niz jeden element oraz zagnieżdżone elementy

w zasadzie wszystko co może przyjąć react component

PropsWithChildren automatically includes children as an OPTIONAL prop

How can you make the children prop mandatory instead of optional when using custom prop types in React?

Instead of using PropsWithChildren (which makes children optional with a question mark), you can manually define children in your type with the type React.ReactNode without the optional operator. For example: type BoxProps = { children: React.ReactNode; width: number; height: number; }

skonczone na quiz Typing useState

Typing Events

When pulling out an event handler function in React with TypeScript, what information does TypeScript require you to provide?

TypeScript requires type hints for the event handler, such as specifying it as a ChangeEventHandler for an HTMLInputElement or a MouseEventHandler for a button element.


What is the recommended approach to discover the correct type for a React event handler or state setter in TypeScript?

Hover over the property or function to see its type definition, then copy that type information rather than trying to manually recreate it.

Skonczone na rozwiązaniu Typing Form Events Exercise

Jeśli robimy component np. Button i chcemy żeby to był superset tego buttona który jest w standard HTML button czyli żeby miał wszystkie propsy jakie ma <button> element 
to możemy skorzystać z ComponentPropsWithoutRef<"button" (albo "form")>

What is the purpose of using ComponentPropsWithoutRef in React with TypeScript when creating custom components that wrap HTML elements?

ComponentPropsWithoutRef allows you to automatically include all the attributes and props from a standard HTML element (like form, button, etc.) in your custom component without having to implement them one by one. This ensures your component supports all native HTML attributes, ARIA tags, event handlers, and other properties of the underlying element.

How can you extend a custom component's props to include all the properties of an HTML form element using TypeScript?

How can you extend a custom component's props to include all the properties of an HTML form element using TypeScript?

Click to reveal answer
You can use ComponentPropsWithoutRef with a generic type parameter specifying the HTML element. For example: ComponentPropsWithoutRef<'form'>. This can be used with an intersection type or by having an interface extend it: interface CounterFormProps extends ComponentPropsWithoutRef<'form'> { }

What is the difference between ComponentProps and ComponentPropsWithoutRef in React TypeScript?

ComponentPropsWithoutRef excludes the ref prop from the component's props, while ComponentProps includes it. ComponentPropsWithoutRef is often preferred because you can handle refs separately using forward ref, higher order components, and useImperativeHandle, giving you more control over ref behavior.

skonczone na dokonczyc form żeby była taka sama jak instruktora


How can you extend TypeScript types to add custom properties to an HTML element component while preserving all original element properties?


You can use the intersection operator with ComponentPropsWithoutRef or similar utility types. For example, if you want to add a 'layout' property to a form component: ComponentPropsWithoutRef<'form'> & { layout?: 'vertical' | 'horizontal' }. This allows the component to accept all standard HTML form properties plus your custom property.




Jeśli mamy sytauacje że nie znamy typu który powinien być w danym miejscu bo mogą być różne to lepiej zamiast any skorzystać z unknown
unknown mówi "You need to check it first"

What is the difference between using any and unknown as a type in TypeScript?

any allows you to do whatever you want with a value without any type checking, causing everything it touches to become any. unknown requires you to perform runtime validation before you can use the value. With unknown, you must check the type (using typeof, instanceof, etc.) before TypeScript will allow you to perform operations on it.


What is a Flux Standard Action and what is its typical structure?

A Flux Standard Action is a convention used in Redux where an action is an object with a type property that contains a string, and a payload property that contains the data. It may also include properties like error or meta.


What does the useReducer hook accept as arguments?

The useReducer hook accepts a reducer function as the first argument (which takes the previous state and an action and returns the new state) and an initial state value as the second argument. It returns an array containing the current state and a dispatch function.


Dlaczego w zadaniu z discriminated unions typescript pozwala na 

interface DecrementAction extends Action {
  type: "decrement";
  payload: never; <------------------------
}

if (action.type === "decrement") {
    const newCount = count - action.payload; <---------------------
    return { count: newCount, draftCount: newCount };
  }

Wersja błędna:
const CounterForm = ({ dispatch }: ActionDispatch<[action: CounterAction]>)

Wersja poprawna:
const CounterForm = ({ dispatch }: { dispatch: ActionDispatch<[action: CounterAction]> })

w wersji błędnej mówimy że cały obiekt propsów ma być funkcją ActionDispatch

a w wersji poprawnej że obiekt propsów ma mieć property "dispatch" pod którą leży funkcja ActionDispatch


Discriminating union pattern is useful for reducers

use() hook from react 19 is for resolving promises inside of a suspense suspender

component will never render until this promise is resovled
react fiber will pause it
const article = use(fetchArticle(id));

with this hook and zod validation we don't have to no longer worry about this value being null

w parent komponencie:

<Suspense fallback={<Loading />}>
          <NewsArticle id={1} />
          <NewsArticle id={2} />
          <NewsArticle id={3} />
          <NewsArticle id={4} />
          <NewsArticle id={5} />
        </Suspense>
		
use() hook działa z suspensem



How does React Suspense handle components that contain unresolved promises?

React Suspense suspends the component rendering when it encounters an unresolved promise. React Fiber will pause the component and display a fallback loading indicator until the promise resolves. The component will never actually render until all promises have resolved, ensuring data is always available when the component executes.



















